FROM python:3.11-slim

WORKDIR /app

COPY scripts/requirements-batch-server.txt .
RUN pip install --no-cache-dir -r requirements-batch-server.txt

COPY scripts/ ./scripts/

EXPOSE 8085

CMD ["uvicorn", "scripts.batch_server:app", "--host", "0.0.0.0", "--port", "8085"]
