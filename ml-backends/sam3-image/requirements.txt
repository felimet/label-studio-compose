# PyTorch: installed via pytorch/pytorch base image in Dockerfile.
# SAM3: installed via `pip install -e /sam3` in Dockerfile.
# label-studio-ml: in requirements-base.txt (git install).

huggingface_hub>=0.27.0
Pillow>=10.3.0
numpy>=1.26.0,<2.0.0
label-studio-sdk>=1.0.0
label-studio-converter>=0.0.57
requests>=2.32.0
# SAM3 runtime dependencies
einops>=0.8.0          # required by SAM3 ViT positional encoding
torchvision>=0.22.0    # required by Sam3Processor (torchvision.transforms.v2)
iopath>=0.1.10         # required by sam3.model_builder (iopath.common.file_io)
pycocotools>=2.0.6     # transitive dep of sam3.train.data.coco_json_loaders (needed at import time)

# SAM3 Agent requirements
openai>=1.0.0          # required for SAM3 Agent LLM interaction
