name: label-anything-sam

# ============================================================
# Local Development Override
# Exposes ports directly; disables strict signup; enables CPU mode.
# This file is auto-loaded by `docker compose up` alongside docker-compose.yml.
# ============================================================

services:
  label-studio:
    ports:
      - "18086:8080"
    environment:
      - LABEL_STUDIO_DISABLE_SIGNUP_WITHOUT_LINK=${LABEL_STUDIO_DISABLE_SIGNUP_WITHOUT_LINK:-true}
      - DJANGO_LOG_LEVEL=INFO
      - LOG_LEVEL=INFO

  minio:
    ports:
      - "19000:9000"   # API (S3 endpoint)
      - "19001:9001"   # MinIO Console UI
      - "19002:9002"   # Full Admin UI (firstfinger/minio only; not via Cloudflare Tunnel)

  pg-db:
    ports:
      - "5433:5432"   # Avoid conflict with local PostgreSQL

  redis:
    ports:
      - "16380:6379"   # Avoid conflict with local Redis

  nginx:
    ports:
      - "18090:80"

  # Expose Batch Annotation Server (for local access)
  batch-server:
    ports:
      - "18085:8085"
